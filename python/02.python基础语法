print("Hello world")  //打印hello world

# 变量赋值
a = 1
a = b = 1

# 标准数据类型
Number (数字)
String (字符串)
List (列表)
Tuple (元组)
Dictionary (字典)

# Python 数字
    # 支持四种不同的数据类型
        int (有符号整型)
        long (长整型)
        float (浮点型)
        complex (复数)

# Python 字符串
    str = "Hello world!"

    print str

# Python 列表
    list = ["a","b","c"]
    map = ["1","2","3"]

    print list          # 输出一个完整的list
    print list[0]       # 打印第一个list元素
    print list[0:2]     # 答应索0-2的元素,包含0不包含2
    print list[1:]      # 从索引为1的元素开始打印,直至末尾
    print list * 2      # 输出列表两次
    print list + map    # 列表联合打印


# Python 字典
    dict = {}
    dict['one'] = "This is one"
    dict[2] = "This is two"

    tinydict = {'name': 'john','code':6734, 'dept': 'sales'}

    print dict['one']          # 输出键为'one' 的值
    print dict[2]              # 输出键为 2 的值
    print tinydict             # 输出完整的字典
    print tinydict.keys()      # 输出所有键
    print tinydict.values()    # 输出所有值

# Python 类型转换
    http://www.runoob.com/python/python-variable-types.html

# Python 运算符
    +       # 加 - 两个对象相加
    -       # 减 - 得到负数或是一个数减去另一个数
    *       # 乘 - 两个数相乘或是返回一个被重复若干次的字符串
    /       # 除 - x除以y
    %       # 取模 - 返回除法的余数
    **      # 幂 - 返回x的y次幂
    //      # 取整除 - 返回商的整数部分(向下取整)

# Python 比较运算符
    ==
    !=
    <>
    >
    <

