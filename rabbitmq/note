rabbitmq 工作模式
  1. simple模式: 简单的收发模式,消息从队列中拿走后自动删除。可以设置为手动的ack,正确处理完后。要手动删除掉,否则会内存溢出
  2. work工作模式: 生产者将消息放入队列中，消费者1、消费者2同时竞争消息。谁先拿到谁负责消费; 为保证消息只被消费1次。可以开启syncronize开关;
  3. publish/subscribe(发布订阅模式): 每个消费者监听自己的队列，消费者将消费发个broker。